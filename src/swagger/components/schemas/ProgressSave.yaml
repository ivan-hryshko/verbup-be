type: object
properties:
  userId:
    type: integer
  words:
    type: array
    items:
      type: object
      properties:
        wordId:
          type: integer
        type:
          type: string
          enum: [ps, pp]
        correct:
          type: boolean
          enum: [true, false]
