description: 'Forbidden'
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 403
        message:
          type: string
          example: 'Please verify your email before login'
